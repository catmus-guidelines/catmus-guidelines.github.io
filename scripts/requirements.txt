# Build dependencies. Lower bounds rather than exact pins: the previous file
# pinned versions that no longer build on current Python (lxml 5.1.0 has no
# wheel for 3.12+ and needs libxml2 headers), which made a fresh checkout
# unbuildable. It also listed several packages nothing imported.
beautifulsoup4>=4.12
Jinja2>=3.1
marko>=2.0
PyYAML>=6.0
jsonschema>=4.21
